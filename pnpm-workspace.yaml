# cdktn is consumed via mutable dist-tags (@next / @latest), so exclude the whole
# CDK Terrain package family from any minimum-release-age gate by name rather than
# pinning a version that would go stale every time @next moves.
minimumReleaseAgeExclude:
  - cdktn
  - cdktn-cli
  - '@cdktn/*'
